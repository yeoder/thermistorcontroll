#ifndef CALCULATION_H
#define CALCULATION_H

#include <cmath>

extern const float R25;  // Deklaration av R25
extern const float B_value;  // Deklaration av B_value

float calculateTemperature(float resistance); // Deklaration

#endif